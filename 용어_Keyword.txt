가상환경 python 경로:
    ver3.9:
        C:\anaconda3\envs\myfirstvirtual1

ssl :
    secure sorket layer

3.9, 3.11 :     
    2가지 버젼으로 가상환경 구성
    conda :
    create , active, deactive

Django, flask, streamlit (lmm과 결합 되었다.):
    파이썬 웹 애플리케이션
folium :
    지도 가져오는 라이브러리
jdk : 
    java development toolkit
패키지 : 
    폴더
모듈 :
    파일 .py이다

iris (아이리스) :
    붖꽃이다.
    종류에는??? 세토사, 버지칼라, 버지니카
    set : 
        setosa, versicolor, virginica

머신러닝 특징 :
    x값과 y값을 구분해야 한다.

$ 여기에 넣는다. $ 
    (라텍스문법:달러달러 사이에 원하는 제곱근, 차수 넣는다.)
    $ax^2 + bx + c = 0     (a=0이면 2차가 아니므로, a\neq0$ )

exponential :
    지수, 거듭제곱, 거듭제곱근
    근(해) : 나온 원인

mining :    
    채굴하다.
    
모든 언어의 자료구조
    1. 선형 :
        배열 (동일한 자료형, 고정된 사이즈),
        리스트 (다른 자료형, 늘어나는),
        스택,
        큐
    2. 비선형 : 
        그래프 (노드, 엣지가 1개 혹은 없어도 된다.),
        트리


.gitignore :
    파일 무시, 파일명으로 인식
reactJs :
    웹컨테이너
docker : 
    컨테이너 -> cloud 개념 -> linux
    요즘은 마이그레이션 자동화... 엔지니어들이 없어도...

쿠버네티스 : 
    키잡이 >> 바다가 떠올라야 한다.
    쿠버네티스는(Kubernetes) 컨테이너 오케스트레이션* 플랫폼으로, 
    컨테이너화된 애플리케이션을 배포, 확장, 관리하기 위한 오픈 소스 도구입니다. 
    간단히 말해 컨테이너화된 애플리케이션을 쉽게 배포하고 관리하기 위한 도구이죠.

tensor :
    고차원 벡터, 차원 높은 행렬
tensorflow : 
    머신러닝

소스트리 :
    깃을 좀 쉽게 사용 가능하다.

github: 
    원격 저장 호스팅 서비스

git에서
    commit id가 중요하다.


추상 :
    코끼리 뼈를 뽑는다.

dot product : 
    A = numpy.array([행렬])
    B = numpy.array([행렬])
    A.dot(B)

대각선 : 
    diagonal

in·ho·mo·ge·ne·ous : 
    /inˌhōməˈjēnēəs/
    1. 불균일한

Attribut information: -> Kaggle 검색 후 컨텐츠에서 나옴
    [칼럼] 을 지칭한다. 
    header [칼럼명], id, 주키(primary key) -> pandas에서도 키로 들어간다.

transpose 트랜스포즈 : 
    행과 열을 변경한다.
매니폴드 : 
    고차원을 단순화 시켜 차원 줄여서 확인한다.
pip :
    python index package
    pip is the package installer for Python.
pwd : 
    print working directory    
psuedo : 
    임시의, 가상의

CRLF:
    캐리지 리턴, 라인 피드

ver 2.12.1 :
    주버젼.부버젼.패치버젼

이번 AI강의 진행방향:
    1. 기준을 잘세워 한다.
    2. 원핫 인코딩 -> 정규화 -> 표준편차 적용 -> 범위 축소, 모델이 잘 만들어 질수 있게, 
    3. pca 분석 후 클러스터링:
        주된 요소 2가지로 평면화 시킨다. 시각화 진행

시멘틱 태그  : 
    header, nav, section, article, aside, footer

yield -> 중간에 리턴, 중간중간 만들기 Generate
    # yield는 return 대신에 쓴다.
    # return 반환하고 종료 한다.
    # return의 단점 해소, 함수에서 로직이 끝나야 실행된다. 그래서 yield 사용
    # 중간에서 업무 멈추고 반환, Generate

쳇GPT, gpt :
    Generative pre-trained transformer 
    생성하는 미리 교육시킨 변환자(encoder, decoder) - 가중치 학습됨.

lambda : 
    이름이 없다. 귀찮다. 익명의 함수
undefined : 
    Node에서 undefined 자료형이 따로 있다. 주소값이 할당되지 않았다.
    String, Boolean, Number, Symbol, 함수, 클래스, undefined

변수 : 
    값을 저장할 때 사용하는 식별자,  
가변 매개변수 : 
    def Func1(*prm1) : <- prm1 가변 매개변수

new 키워드 : 
    새로운 주소를 부여한다.

` : 빽틱, 악센트
~ : tilde

console:
    입출력 장치
NPM :
    node package manager

cdm 방식 :
    contents
PX (picture element): 
    모니터 화면을 구성하는 사각형 1개의 크기
&nbsp; :
    non-breaking space 
tag 테그 : 
    == 선택자 (selector)
    
* (애스터리스크): aesterisk
    정규표현식 (Regular expression)
ERD:
    엔티디 관계 다이어그램
# : 
    아이디 (유일한 값)
. :
    틀, 클래스
href :
    Hyper(건너뛴다.)text Reference
CSS : 
    Cascading Style Sheet
cascading : 
    깔맞춤. 한복은 한복, 양복은 양복

head -> body위
heading : 소제목
header -> semantic 태그에 있다. 
footer

math.sqrt 는 (내용물)**1/2 과 같다.

행 : row,  열 : column

선형대수 : 선모양 linear   
    유클리드 (y = ax+b )
    1차방정식, 1차 함수, 기하, 행렬과 연결되어 있다.
    행렬 < 벡터 < 스칼라 (벡터가 여러 개)

    공간 한정, 2중 for구문 사용,
    면 개념 사용 후 반복
기하학:
    이집트 메소포타미아, 니 땅 내땅 구분하기 위해 나옴, 점선면 체크.

맨하튼 (|x1-x2| + |y1-y2|), 민코프스키 (곡선)

클래스
스키마(틀) : 틀을 만들어서 저장.

클래스
    변수 : field
    함수 : method

data lake : 
    비정형 데이터의 모음

data단위
    저장 단위 : bit < byte < kilo < mega < giga < tera < peta < exa < zeta < yota
    표현 단위 : bit, nibble, byte, word, field (영역)(열), record (행) , file , database, data warehouse

method :
    방법, 방식

form 태그 : 
    자료를 넣는 폼

www.naver.com/robots.txt
    User-agent: *
    Disallow: /
    Allow : /$
    Allow : /.well-known/privacy-sandbox-attestations.json

네이버 로그인:
    개발자모드에서 nidlogin.login

MSA : 
    microservice architecture 			

DBMS :
    오라클/mysql, mssql, mariaDB, 티베로

preserve log :
    로그를 유지해서 출력해라.

IDE : 
    통합개발환경, integrated deveopment enviornment 